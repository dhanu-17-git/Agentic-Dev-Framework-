# Compact Feature Contract Schema

> **Rule:** Max 8-12 lines per contract. JSON only.

```json
{
  "task": "feature_name",
  "files": ["file1.ext", "file2.ext"],
  "changes": ["action1", "action2"],
  "dependencies": ["dep1"],
  "risk": "low|medium|high",
  "notes": "1 line reasoning / trace tracking"
}
```
