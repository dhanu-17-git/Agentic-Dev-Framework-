# Refactoring Workflow

## Steps
1. Analyze current code for duplication, complexity, and smell
2. Define refactor scope — what changes, what stays the same
3. Improve structure without changing observable behavior
4. Run existing tests to confirm no regressions
5. Update documentation if module responsibilities changed
